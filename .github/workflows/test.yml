name: Lint and test

on:
  push:
    branches: [ '*' ]

jobs:
  lint-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Install
        run: corepack enable && yarn install
      - name: Lint
        run: yarn lint
      - name: Test
        run: yarn test
